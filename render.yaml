# minigpt4-backend/render.yaml
services:
  - type: web
    name: minigpt4-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000